Name:           ros-jade-ecl-mobile-robot
Version:        0.60.0
Release:        0%{?dist}
Summary:        ROS ecl_mobile_robot package

Group:          Development/Libraries
License:        BSD
URL:            http://wiki.ros.org/ecl_mobile_robot
Source0:        %{name}-%{version}.tar.gz

Requires:       ros-jade-ecl-errors
Requires:       ros-jade-ecl-formatters
Requires:       ros-jade-ecl-geometry
Requires:       ros-jade-ecl-license
Requires:       ros-jade-ecl-linear-algebra
Requires:       ros-jade-ecl-math
BuildRequires:  ros-jade-catkin
BuildRequires:  ros-jade-ecl-errors
BuildRequires:  ros-jade-ecl-formatters
BuildRequires:  ros-jade-ecl-geometry
BuildRequires:  ros-jade-ecl-license
BuildRequires:  ros-jade-ecl-linear-algebra
BuildRequires:  ros-jade-ecl-math

%description
Contains transforms (e.g. differential drive inverse kinematics) for the various
types of mobile robot platforms.

%prep
%setup -q

%build
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/jade/setup.sh" ]; then . "/opt/ros/jade/setup.sh"; fi
mkdir -p obj-%{_target_platform} && cd obj-%{_target_platform}
%cmake .. \
        -UINCLUDE_INSTALL_DIR \
        -ULIB_INSTALL_DIR \
        -USYSCONF_INSTALL_DIR \
        -USHARE_INSTALL_PREFIX \
        -ULIB_SUFFIX \
        -DCMAKE_INSTALL_PREFIX="/opt/ros/jade" \
        -DCMAKE_PREFIX_PATH="/opt/ros/jade" \
        -DSETUPTOOLS_DEB_LAYOUT=OFF \
        -DCATKIN_BUILD_BINARY_PACKAGE="1" \

make %{?_smp_mflags}

%install
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/jade/setup.sh" ]; then . "/opt/ros/jade/setup.sh"; fi
cd obj-%{_target_platform}
make %{?_smp_mflags} install DESTDIR=%{buildroot}

%files
/opt/ros/jade

%changelog
* Tue Oct 20 2015 Daniel Stonier <d.stonier@gmail.com> - 0.60.0-0
- Autogenerated by Bloom

